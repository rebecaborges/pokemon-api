const {
  getPokemons,
  createPokemons,
  buyPokemons,
  deletePokemons,
} = require('./pokemon')

const {
  getInsignias,
  createInsignias,
} = require('./insignia')

module.exports = {
  getPokemons,
  createPokemons,
  buyPokemons,
  deletePokemons,
  getInsignias,
  createInsignias,
}
